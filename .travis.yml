dist: xenial

language: rust

services:
    - docker

before_install:
    - docker -v
    - docker build --rm -t zboxfs/nodejs .
    - docker images

script:
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
    - docker push zboxfs/nodejs
